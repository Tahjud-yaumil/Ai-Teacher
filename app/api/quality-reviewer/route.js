import { NextResponse } from 'next/server';
export const runtime='nodejs'; export const dynamic='force-dynamic'; export const maxDuration=300;
const cleanDate=(v)=>{const s=String(v||'');const m=s.match(/\b(20\d{2})[-/](\d{2})[-/](\d{2})\b/);if(m)return `${m[1]}-${m[2]}-${m[3]}`;const d=new Date(v);return Number.isNaN(d.getTime())?new Date().toISOString().slice(0,10):new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Jakarta'}).format(d)};
export async function POST(req){try{const body=await req.json().catch(()=>({}));const tanggal=cleanDate(body?.tanggal);const host=req.headers.get('host'),proto=req.headers.get('x-forwarded-proto');const origin=proto&&host?`${proto}://${host}`:`https://${process.env.VERCEL_URL}`;const r=await fetch(`${origin}/api/quality-reviewer-v1?run=${Date.now()}`,{method:'POST',headers:{'content-type':'application/json','cache-control':'no-cache'},body:JSON.stringify({tanggal,use_ai:body?.use_ai!==false}),cache:'no-store'});const data=await r.json().catch(()=>({agent:'quality_reviewer',status:'error',reason:'Respons reviewer bukan JSON.'}));return NextResponse.json(data,{status:r.status});}catch(e){return NextResponse.json({agent:'quality_reviewer',status:'error',reason:e instanceof Error?e.message:'Quality Reviewer gagal.'},{status:500});}}
